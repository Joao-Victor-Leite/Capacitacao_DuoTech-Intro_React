import styled from 'styled-components'

export const LoginContent = styled.body`
    min-height: 100vh;
    font-size: 1em;
    align-items: center;
    justify-content: center;
    display: flex;
`;

export const LoginContainer = styled.div`
    height: 16em;
    width: 15em;
    background: var(--bg-login-color);
    color: var(--text-color);
    border-radius: 5%;
`;
